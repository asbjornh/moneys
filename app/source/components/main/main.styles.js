import { StyleSheet } from "react-native";

import { darkGray } from "../../styles/vars";

export default StyleSheet.create({
  safeAreaWrapper: {
    flex: 1,
    backgroundColor: darkGray
  },
  wrapper: {
    flex: 1,
    paddingHorizontal: 20,
    paddingBottom: 50
  }
});
